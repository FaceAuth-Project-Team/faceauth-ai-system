# --- Image Settings ---
# Everyone MUST use these dimensions so the model doesn't crash
IMG_SIZE = (64, 64) 
GRAYSCALE = True

# --- ML Settings ---
CONFIDENCE_THRESHOLD = 0.8  # Minimum match score to 'Grant Access'